<Page x:Class="MainPCDoctor.Desktop.Views.WhyWasSlowView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgDeepBrush}"
      Title="Why Was Slow">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="WHY WAS MY PC SLOW?"
                   Style="{StaticResource HeadingMedium}" Margin="0,0,0,16" />

        <!-- Summary -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <TextBlock x:Name="SummaryText" Style="{StaticResource BodyText}"
                       Text="Select a time range and press Analyze." TextWrapping="Wrap" />
        </Border>

        <!-- Analyze button -->
        <Button Grid.Row="2" Content="Analyze Last 24 Hours" Style="{StaticResource PrimaryButton}"
                HorizontalAlignment="Left" Margin="0,0,0,16"
                Click="AnalyzeButton_Click" />

        <!-- Results list -->
        <ListView Grid.Row="3" x:Name="ResultList"
                  Background="{StaticResource BgSurfaceBrush}"
                  BorderBrush="{StaticResource BorderSubtleBrush}"
                  BorderThickness="1"
                  Foreground="{StaticResource TextPrimaryBrush}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time"     Width="160" DisplayMemberBinding="{Binding StartedAt}" />
                    <GridViewColumn Header="Type"     Width="160" DisplayMemberBinding="{Binding BottleneckType}" />
                    <GridViewColumn Header="Duration" Width="100" DisplayMemberBinding="{Binding DurationSeconds}" />
                    <GridViewColumn Header="Resolved" Width="100" DisplayMemberBinding="{Binding ResolvedAt}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
