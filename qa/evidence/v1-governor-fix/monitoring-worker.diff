diff --git a/qa/evidence/v1-governor-fix/MonitoringWorker.before.cs.txt b/src/MainPCDoctor.Desktop/Background/MonitoringWorker.cs
index 4f1ca2d..03bb6f9 100644
--- a/qa/evidence/v1-governor-fix/MonitoringWorker.before.cs.txt
+++ b/src/MainPCDoctor.Desktop/Background/MonitoringWorker.cs
@@ -21,6 +21,9 @@ public sealed class MonitoringWorker : BackgroundService
 
     private SamplingState _state        = SamplingState.Normal;
     private bool          _ramWired;
+    internal Func<bool> IsUnderPressure { get; init; } = ResourceGovernor.ShouldThrottle;
+    internal Func<TimeSpan, CancellationToken, Task> DelayAsync { get; init; }
+        = (interval, ct) => Task.Delay(interval, ct);
 
     public MonitoringWorker(
         ISystemMetricsCollector    collector,
@@ -49,47 +52,62 @@ public sealed class MonitoringWorker : BackgroundService
         await _retention.RecoverOrphansAsync(stoppingToken).ConfigureAwait(false);
 
         // Schedule daily retention once at midnight
-        _ = RunDailyRetentionAsync(stoppingToken);
+        using var retentionCancellation = CancellationTokenSource.CreateLinkedTokenSource(stoppingToken);
+        var retentionTask = RunDailyRetentionAsync(retentionCancellation.Token);
 
-        while (!stoppingToken.IsCancellationRequested)
+        bool wasUnderPressure = false;
+        try
         {
-            var interval = SamplingScheduler.GetInterval(_state);
-            await Task.Delay(interval, stoppingToken).ConfigureAwait(false);
-
-            if (ResourceGovernor.ShouldThrottle())
-            {
-                _logger.LogWarning("ResourceGovernor throttling — skipping cycle");
-                continue;
-            }
-
-            try
+            while (!stoppingToken.IsCancellationRequested)
             {
-                var snapshot = await _collector.CollectAsync(stoppingToken).ConfigureAwait(false);
-                _buffer.Add(snapshot);
-                await _aggregator.TryFlushAsync(stoppingToken).ConfigureAwait(false);
-
-                // Wire total RAM into diagnosis engine on first real snapshot
-                if (!_ramWired && snapshot.Memory.TotalPhysicalGb > 0)
+                bool underPressure = IsUnderPressure();
+                if (underPressure != wasUnderPressure)
                 {
-                    if (_diagnosis is DiagnosisEngine de)
-                        de.SetTotalPhysicalRam(snapshot.Memory.TotalPhysicalGb);
-                    _ramWired = true;
+                    if (underPressure)
+                        _logger.LogWarning("ResourceGovernor reducing sampling to 60 seconds; monitoring remains active");
+                    else
+                        _logger.LogInformation("ResourceGovernor recovered; resuming normal sampling");
+                    wasUnderPressure = underPressure;
                 }
+                var interval = underPressure
+                    ? ResourceGovernor.PressureInterval
+                    : SamplingScheduler.GetInterval(_state);
+                await DelayAsync(interval, stoppingToken).ConfigureAwait(false);
 
-                var window = _buffer.GetRecent(seconds: 600);
-                var result = _diagnosis.Evaluate(window);
-                await _tracker.UpdateAsync(result, stoppingToken).ConfigureAwait(false);
-
-                _state = SelectNextState(result);
-            }
-            catch (OperationCanceledException) { break; }
-            catch (Exception ex)
-            {
-                _logger.LogError(ex, "Monitoring cycle error");
+                try
+                {
+                    var snapshot = await _collector.CollectAsync(stoppingToken).ConfigureAwait(false);
+                    _buffer.Add(snapshot);
+                    await _aggregator.TryFlushAsync(stoppingToken).ConfigureAwait(false);
+
+                    // Wire total RAM into diagnosis engine on first real snapshot
+                    if (!_ramWired && snapshot.Memory.TotalPhysicalGb > 0)
+                    {
+                        if (_diagnosis is DiagnosisEngine de)
+                            de.SetTotalPhysicalRam(snapshot.Memory.TotalPhysicalGb);
+                        _ramWired = true;
+                    }
+
+                    var window = _buffer.GetRecent(seconds: 600);
+                    var result = _diagnosis.Evaluate(window);
+                    await _tracker.UpdateAsync(result, stoppingToken).ConfigureAwait(false);
+
+                    _state = SelectNextState(result);
+                }
+                catch (OperationCanceledException) { break; }
+                catch (Exception ex)
+                {
+                    _logger.LogError(ex, "Monitoring cycle error");
+                }
             }
         }
-
-        _logger.LogInformation("MonitoringWorker stopped");
+        catch (OperationCanceledException) when (stoppingToken.IsCancellationRequested) { }
+        finally
+        {
+            retentionCancellation.Cancel();
+            await retentionTask.ConfigureAwait(false);
+            _logger.LogInformation("MonitoringWorker stopped");
+        }
     }
 
     private async Task RunDailyRetentionAsync(CancellationToken ct)
